# PROMPT-SUP-G7 - Coding Prompt

Implement SUP-G7 in suppliers-microservice. Add a validated Warehouse supplier reconciliation client path for normalized stock candidates. The path must require supplierSku, productId, warehouseId, non-negative stockQuantity, actor, reason, idempotency key, and approval before any Warehouse mutation is attempted. It must call Warehouse `POST /api/supplier-reconciliations` with supplierId, warehouseId, productId, quantity, externalReference, actor, and observedAt using a bearer token from runtime env. Preserve default import behavior as non-mutating. Do not add supplier-specific API adapters, Catalog writes, production mutations, secrets, or deployment. Validate with pre-coding gate, npm build, synthetic compiled checks, strict doc audit, deployment readiness gate, and git diff check.
